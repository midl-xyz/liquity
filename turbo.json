{
	"$schema": "https://turbo.build/schema.json",
	"tasks": {
		"start": {
			"dependsOn": ["^build"],
			"persistent": true
		},
		"build": {
			"dependsOn": ["^build"],
			"outputs": ["dist"]
		}
	}
}